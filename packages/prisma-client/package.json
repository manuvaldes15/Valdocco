{
  "name": "@valdocco/prisma-client",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push",
    "seed": "ts-node --transpile-only prisma/seed.ts",
    "build": "prisma generate && tsc -p tsconfig.json"
  },
  "dependencies": {
    "@prisma/client": "5.22.0"
  },
  "devDependencies": {
    "bcryptjs": "^2.4.3",
    "@types/bcryptjs": "^2.4.6",
    "prisma": "5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
